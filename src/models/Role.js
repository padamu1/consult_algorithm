import React, {Component} from 'react';

class Role extends Component {
    w_actor = {
        w_entity,
        w_arrows,
        w_switch,
        w_onoff,
        w_panel
    }
    constructor(porps){
        super(props);
    }
    componentDidMount(){
        //외부 라이브러리 연동 or 컴포넌트에서 필요한 데이터 요청 할것
    }
}